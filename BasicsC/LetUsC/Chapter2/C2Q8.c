#include<stdio.h>
int main(){
    int C, D;
    printf("Enter any two number\n");
    scanf("%d %d", &C, &D);

    printf(" after interchanging the value of C is %d and value of D is %d", D, C);
    return 0;
}